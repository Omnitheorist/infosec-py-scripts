# A simple looping script to hash values.
import hashlib
while True:
    plaintext = input("Enter plaintext to be hashed: ")
    hashlib.en
    result = hashlib.md5(plaintext)
    print(result)
